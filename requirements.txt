# Interface (pywebview no Windows traz cffi/clr_loader/pythonnet/bottle
# automaticamente como sub-dependências — não precisa listar à mão)
pywebview==6.2.1

# Imagens
pillow==12.3.0

# Documentos (TXT/PDF/DOCX/HTML/RTF)
python-docx==1.2.0
pypdf==6.16.2
xhtml2pdf==0.2.17
striprtf==0.0.33

# Dados (XLSX/YAML — CSV/JSON/XML já são stdlib)
openpyxl==3.1.5
pyyaml==6.0.3

# Build do .exe
pyinstaller==6.22.2
